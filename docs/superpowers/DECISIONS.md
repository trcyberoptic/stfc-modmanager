# Decision log — initial implementation

This is the controller's ledger from the subagent-driven build of the first
release. It records every ruling made on the plan's behalf, every finding a
review raised, and every finding deliberately deferred, with what each would
cost if the call was wrong. Kept because roughly twenty of the defects below
were found by review rather than by the plan, and the reasoning is worth more
than the summary.

---

# SDD ledger — plan: docs/superpowers/plans/2026-08-15-stfc-modmanager.md

Spec: docs/superpowers/specs/2026-08-15-stfc-modmanager-design.md (read, binding authority)
Branch: feat/initial-implementation
Base commit: e16ed9a

## Pre-flight conflict scan

### Shared files / interfaces between tasks

| Producer → Consumer | What crosses | Finding |
|---|---|---|
| 1 → 2..10 (`SelfTest.cs`) | `Check`/`Eq` harness, cumulative assert count | Counts drift: Task 4 adds 12 checks but claims 26 (13+12=25). See R2. |
| 1 → 11, 12 (`Program.cs`) | `Main` body | Sequential edits, no conflict. Task 1's own text conflicts with itself: code block shows non-partial class, note requires `partial` for `LibraryImport`. See R1. |
| 2 → 6, 9, 10, 12 | `GameInstall`, `GameLocator.*` | Consistent. `IsWritable` produced in 2, consumed in 9. Clean. |
| 3 → 9, 12, 13 | `ModInspector.Read`, `PluginInfo` | Consistent. |
| 4 → 13 | `MapResult`, `MappedFile`, `MapArchive/MapSingleFile` | Consistent. Empty-file case is guarded by the rejection path before `map.Files[0]`. Clean. |
| 5 → 6, 7, 9, 10, 12, 13 | `AppState`, `ModEntry`, `InstalledFile`, `SharedFile`, `AppPaths`, `AppLog`, `GitHubToken` | Consistent. |
| 6 → 12, 13 | `Apply` → `IReadOnlyList<InstalledFile>`, `RegisterShared`, `ReleaseShared`, `SetEnabled`, `Remove`, `PruneBackups` | `Remove` only iterates `mod.Files`; Task 13 records non-main files as shared only, so uninstall orphans them. See R5. |
| 7 → 11, 13 | `ReleaseInfo`, `ReleaseAsset`, `GetLatestReleaseAsync`, `PickAsset`, `DownloadAssetAsync` | Task 13 blocks on these with `.GetAwaiter().GetResult()` from the WinForms UI thread. See R4. |
| 8 → 9, 10 | `BepInExRuntime.Detect` | Consistent; 8 precedes 9. |
| 9 → 10, 12 | `HealthCheck.Run`, `Finding`, `Severity`, `LogReader` | Consistent. |
| 10 → 12 | `SupportBundle.Create`, `PlannedContents` | Consistent. |
| 11 → 12 | `SelfUpdate.CleanupOldExecutable` in `Main` | Consistent. |
| 12 → 13 | `MainForm` calls `Dialogs.AddFromGitHub/InstallLocalPath/CheckUpdatesAsync/SetUpdateSource` | **`Ui/Dialogs.cs` does not exist until Task 13 — Task 12 cannot compile on its own.** See R3. |
| 13 → 12 | `Dialogs` calls back into `MainForm`-owned state only via parameters | Clean, no circular state. |

### Per-task internal consistency

| Task | Tests vs. code | Files created vs. later touched | Finding |
|---|---|---|---|
| 1 | harness asserts itself | `Program.cs`, `SelfTest.cs`, csproj | R1 (partial class) |
| 2 | 6 asserts, all against `ParseGamePathFromIni` | consistent | clean |
| 3 | 6 asserts against `DecodeStringArgs` only; `Read` covered by a manual probe | consistent | acceptable — `Read` needs a real DLL, probe is Step 5 |
| 4 | 12 asserts vs. claimed 26 total | consistent | R2 |
| 5 | 5 asserts on serialization round trip | consistent | clean |
| 6 | 7 asserts on refcounting; `Apply` rollback untested | consistent | acceptable — rollback needs real file I/O; covered by Task 13 end-to-end probe |
| 7 | 13 asserts on the two pure functions | consistent | clean |
| 8 | 2 asserts, builds a real zip in temp | consistent | clean |
| 9 | 11 asserts | consistent | clean |
| 10 | 8 asserts on `RedactLine` | consistent | clean |
| 11 | no asserts, manual probe only | modifies `Program.cs` from Task 1 | acceptable — rename-while-running cannot be asserted in-process |
| 12 | no asserts; manual verification | `AdoptFromDisk` native-entry branch chain has an unreachable third arm | R6 |
| 13 | no asserts; end-to-end probe | consistent | R4 applies |
| 14 | CI runs `--selftest` | consistent | Task 14 Step 4 is a publish → stop and ask |

## Rulings

- **R1** — Task 1: `Program` must be declared `internal static partial class`; the `LibraryImport` source generator requires `partial` on both method and containing class. The plan's note is authoritative over its code block. Cost if wrong: none, the alternative does not compile.
- **R2** — The "Erwartet: N passed" numbers in the plan's test steps are advisory. The binding condition is **`0 failed` and exit code 0**. Task 4's figure is arithmetically off by one. Cost if wrong: none.
- **R3** — Tasks 12 and 13 are dispatched as **one unit**. `MainForm` references `Dialogs`, which Task 13 creates; Task 12's build step cannot pass alone, and stubbing `Dialogs` would only be thrown away. Cost if wrong: one larger review surface instead of two smaller ones.
- **R4** — Every `await` in `Core` (`GitHubClient`, `BepInExRuntime`, `SelfUpdate`) uses `.ConfigureAwait(false)`. Task 13 calls these synchronously from the WinForms UI thread via `.GetAwaiter().GetResult()`; without it the continuation is posted back to the blocked UI thread and the app deadlocks. Cost if wrong: none, `ConfigureAwait(false)` is strictly safer in library code.
- **R5** — `Installer.Remove` additionally releases every `SharedFile` whose `Providers` contains the mod's id, deleting the file when the list empties. Without it, Task 13's split (main DLL in `mod.Files`, everything else in `SharedFiles`) leaves the extra files behind forever. Carried into Task 6. Cost if wrong: over-deletion, bounded by the refcount that already guards it.
- **R6** — In `MainForm.AdoptFromDisk`, restructure the native-mod branch to `if (!present) remove-if-any; else if (entry is null) add; else update Enabled`. The plan's third arm is unreachable, so a deleted `version.dll` would leave a stale row. Carried into Task 12. Cost if wrong: cosmetic list entry.
- **R7** — The .NET 10 SDK install (Task 1, Step 1) runs without a separate confirmation: the user chose to execute this plan and the SDK is its first step. Cost if wrong: a developer SDK installed on the user's own machine, removable via winget.
- **Stop point** — Task 14, Step 4 creates a public GitHub repository. That is a publish; execution halts there and asks. **RESOLVED 2026-08-16: the user explicitly approved creating the public repo `trcyberoptic/stfc-modmanager`. The stop point is lifted.**
- **User approval 2026-08-16:** the single-instance guard in `Program.cs` (Task 12) is confirmed wanted, not merely a controller ruling.
- **SHUTDOWN 2026-08-16 00:55 while Task 6's fix round was mid-edit.** `HEAD` is `8f435a0` (Task 6 implementation, pre-fix). The working tree carried an unfinished fix round: modified `Core/AppState.cs`, `Core/Installer.cs`, `Program.cs`, `SelfTest.cs`, plus an untracked scratch harness `src/StfcModManager/DevVerifyTask6Round1.cs`.
- **RESUMED 2026-08-16 19:06. Earlier instruction to discard the tree REVERSED on measurement.** The abandoned work builds with 0 warnings/0 errors and passes 91 asserts (up from 84), exit 0 — it is substantially complete, and the scratch harness still being present says the agent was killed during sandbox verification, not during editing. Discarding 319 lines of a Critical security fix on suspicion alone was the wrong call. Snapshot saved as `abandoned-task6-round1.patch` + `DevVerifyTask6Round1.cs` in this directory. Ruling: salvage under audit — a fresh implementer must verify each of the eight findings against the code itself rather than assume, and Task 6's re-review is a FULL review of `8498081..HEAD`, not a scoped fix-delta review, because the provenance of these edits is unverified. Cost if wrong: the full re-review catches what the audit misses, which is exactly the case the scoped review would have missed.
- **PAUSED 2026-08-16 by the user after Task 6's fix round was dispatched.** Do not dispatch further work without the user's word. Resume point: re-review the Task 6 fix diff (scoped, findings C1, I1+M4, I2, I3, I4, I5, M1, M2+M3), then continue at Task 7. Briefs for Tasks 3-6 are already extracted in this directory.

## Progress

Task 1: complete (commits e16ed9a..d1995b0, review clean — spec ✅, quality approved)
Task 1: deviations accepted — `AllowUnsafeBlocks=true` (LibraryImport/SYSLIB1062, no smaller fix exists, verified by the reviewer), repo-local `nuget.config` with `<clear/>` + nuget.org only (machine had zero package sources; needed for the SDK's own runtime packs under self-contained publish, not for third-party packages).
Task 1: minor (deferred): `SelfTest._passed/_failed` are static and never reset — safe while `Run()` is called once per process.
Task 1: minor (deferred): `dotnet run -- --selftest` prints nothing in this shell (Debug apphost + AttachConsole); the published exe prints correctly and the exit code is authoritative either way.

Task 2: review — spec ✅, quality NOT approved (2 Important, both originating in the plan's own code block).
Task 2: Ruling: `ParseGamePathFromIni` returning `" "` for a whitespace-only value — the plan's code is wrong against its own documented contract ("null when nothing valid"), and the spec makes this a published interface. Fix it (trim + `IsNullOrWhiteSpace`) and add a covering assert. Cost if wrong: none, the caller already tolerated both behaviours.
Task 2: Ruling: `Detect()` catching only `IOException` while `File.ReadAllLines` also throws `UnauthorizedAccessException` — an unreadable launcher file would crash startup, contradicting the method's own contract and its sibling methods. Fix it. Cost if wrong: none, a swallowed permission error falls through to the same fallback path the method already has.
Task 2: minor (deferred): `IsWritable` can leave `.stfcmm-write-probe` behind if the delete fails after a successful write.
Task 2: fix round 1/5 (2 addressed, 0 open; commits 2c121e0..4380cfb)
Task 2: complete (commits d1995b0..4380cfb, review clean — 8 asserts, exit 0)

Task 3: review — spec ✅, 2 Important despite the reviewer's "approved" verdict; both entered the fix loop.
Task 3: Ruling: `ModInspector.Read`'s catch filter misses `ArgumentException`/`NotSupportedException` from `File.OpenRead`. Reachable — Task 13 feeds it drag-and-dropped user paths. Fix. Cost if wrong: none; a swallowed path error returns the same null the contract already promises.
Task 3: Ruling: `TryReadCompressedUInt`'s 2-byte and 4-byte branches have zero automated coverage. Binary parser, silent-regression risk. Add four asserts. Cost if wrong: none.
Task 3: minor (deferred): `version ?? "0.0.0"` in `Read` is unreachable — `version` is assigned in the same branch as `guid`.
Task 3: minor (deferred): `AttributeTypeName` matches the short type name only; a same-named attribute from an unrelated assembly would false-positive. Irrelevant for a curated mod folder.
Task 3: minor (accepted, plan-mandated): `0xFF` terminates the whole decode loop instead of one argument — no observable effect on BepInEx's actual attribute shapes.
Task 3: minor (accepted, in scope): `Read` itself has no automated coverage; only the pure decoder does. Evidence is the real-DLL probe — 7 plugins found, 4 shared libraries correctly rejected.
Task 3: probe verified reverted — `Program.cs` clean in the working tree, not merely claimed.
Task 3: fix round 1/5 (2 addressed, 0 open; commits dfe8704..40d131a) — re-reviewer hand-verified the new asserts are non-vacuous (0x80 0xC8 really encodes 200; the truncated 4-byte case really trips the 4-byte bounds check).
Task 3: complete (commits 4380cfb..40d131a, review clean — 19 asserts, exit 0)

Task 4: review (opus, adversarial) — spec ✅, quality NOT approved. No Critical: the escape boundary itself held against ~110 hostile inputs. 4 Important.
Task 4: implementer closed two real bypasses beyond the brief — trailing dot on a blocked extension (`setup.exe.`, Windows strips the dot on write) and a colon anywhere in the name (NTFS alternate data stream, `MyMod.exe:hidden.dll` reports extension `.dll`). Both confirmed by the reviewer, both kept.
Task 4: Ruling: its third added rule (dots-only segments) rests on a claim the reviewer empirically disproved — `BepInEx\...\x` throws, it does not collapse — and it falsely rejects `./MyMod.dll`, which Windows' own bundled `tar.exe` emits for `tar -a -cf mod.zip .`. Narrow the rule to exempt a bare `.` and correct the comment that states the false OS behaviour. Cost if wrong: an archive shape nobody ships would slip to the next check, which already rejects it.
Task 4: Ruling: `MapArchive` catches only `InvalidDataException`, so a locked zip — the normal state right after a download while AV scans it — throws out of the method into a caller with no matching filter. Fix. Cost if wrong: a rejection message instead of a crash.
Task 4: Ruling: `MappedFile.Entry` stores the normalised name, but Task 13 looks the entry up in the zip by that string and skips silently on miss. A real archive with backslash separators would be shown in the trust dialog, approved by the user, and then not installed. Keep the raw name in `Entry`. Cost if wrong: none — `Target` keeps carrying the normalised form.
Task 4: Ruling: colliding targets are produced and never detected (`Release/Mod.dll` + `Debug/Mod.dll`, and assets flattened in mixed BepInEx archives), which double-writes and corrupts the §6.4 reference counting downstream. Reject on duplicate targets rather than de-duplicate — silently dropping a file is worse than refusing the package. Cost if wrong: an ambiguous archive is refused with a clear message instead of installed by guess.
Task 4: Ruling: entries with characters illegal on Windows currently pass and blow up later as an unhandled `ArgumentException` in `Installer.ResolveInside`. Same class as the escape above, so fold it into this round. Cost if wrong: none, no legitimate archive carries them.
Task 4: minor (deferred): `hasBepInEx && idx >= 0` — the first term is dead by construction.
Task 4: minor (deferred): the trailing-space assert passes against the pre-fix code too (`.Trim()` already handled it) — it documents intent but does not exercise the new rule.
Task 4: minor (deferred): reserved device names (`NUL.dll`, `COM1.dll`) are accepted here and fail late in `ResolveInside` with a misleading "escapes the game folder" message. No breach.
Task 4: minor (deferred): `MapSingleFile` on a directory path reports "archive contains no installable files" — wrong noun.
Task 4: minor (deferred): archive casing is preserved into stored paths (`bepinex\plugins\…`); harmless while all downstream comparisons are OrdinalIgnoreCase.
Task 4: minor (deferred): blocklist omits `.hta .pif .scf .url .cpl .msc .wsf .vbe .jse`; the spec's mandated nine are all covered.
Task 4: known limitation (accepted): the plan's mapping flattens non-`BepInEx` subdirectories to the basename, so an archive relying on its own folder layout cannot be represented. The duplicate-target rejection now surfaces this loudly instead of losing files silently.
Task 4: fix round 1/5 (5 addressed, 2 new open; commits 79ccca2..d3fb90a) — re-reviewer verified F1–F5 against real zips and a real `tar.exe` archive, and confirmed the escape boundary intact. The F1 fix exempted `.` from the traversal check without ever removing it, which reintroduced an unhandled `IndexOutOfRangeException` on an entry of exactly `"."` and let `BepInEx/./plugins/A.dll` slip past the new collision check as a distinct string that resolves to the same file.
Task 4: Ruling: fix N1 and N2 at the shared root — drop `.` segments during normalisation instead of exempting them, and skip an entry that has nothing left. Cost if wrong: an entry shape no archiver emits is skipped rather than mapped.
Task 4: minor (deferred): the archive-rejection message appends the OS-localised `e.Message` to English text. — fixed in round 2.
Task 4: fix round 2/5 (2 addressed, 0 open; commits d3fb90a..bd93de8) — re-reviewer fuzzed 13k single and 55M paired entry names: 0 throws, 0 targets containing a literal `.` segment, 0 escapes, and a 1,260-pair equivalence fuzz found no two accepted entries resolving to the same file.
Task 4: minor (deferred): an entry made only of `/` and `.` is now silently skipped instead of rejected as absolute — fails closed, only the diagnostic is lost.
Task 4: minor (deferred): an entry starting `.//` now collapses to a leading `/` and is rejected as absolute — a shape no real archiver emits, fails closed.
Task 4: minor (deferred): the `s != "."` guard in the traversal condition is now unreachable.
Task 4: complete (commits 40d131a..bd93de8, review clean — 55 asserts, exit 0)

Task 5: review — spec ✅, quality NOT approved (2 Important). Implementer found and fixed two real defects beyond the brief (`"Mods": null` nulled the list; `Load()`'s catch was too narrow) and correctly chose to let `Save()` propagate failures rather than swallow them.
Task 5: Ruling: `Save()` uses a fixed `state.json.tmp`, so two instances can silently persist each other's content. Fix with a per-process, per-call unique temp name; keep the same-directory rename. Cost if wrong: none, the rename stays atomic either way.
Task 5: Ruling: null elements inside `Mods`/`SharedFiles`/`TrustedRepos` survive deserialisation and throw downstream. Same class as the top-level fix already applied; generalise it. Cost if wrong: none.
Task 5: Ruling: a corrupt state file silently discards the user's whole mod inventory and the next save overwrites it. The fallback stays, but `Load()` must log it and move the unreadable file aside first. Cost if wrong: a stray `state.json.corrupt-*` file next to the state.
Task 5: Ruling carried into Task 12: add a single-instance guard in `Program.cs`. Nothing today stops a second manager instance from writing into the same game folder. Cost if wrong: a user with a genuine reason to run two instances is blocked.
Task 5: note: the implementer's BOM justification was factually wrong — `File.ReadAllText` already strips a UTF-8 BOM, so the scenario cannot arise through `Load()`. The defensive strip in the pure function is harmless and stays.
Task 5: minor (deferred): the temp file is not removed when `Move` throws; the next successful save overwrites it.
Task 5: minor (deferred): `PruneOldLogs` enumerates the log directory on every single log write.
Task 5: fix round 1/5 (3 addressed, 1 new open; commits 0e76ceb..f0a6245) — implementer verified F1 and F3 against the real filesystem (two processes racing 500 saves each; a hand-corrupted state file quarantined with a matching log line). The unique temp name removed the old fixed name's self-cleaning property, so a failed save now leaves a permanent orphan.
Task 5: Ruling: close the orphan leak without weakening the propagation contract — delete the temp best-effort and rethrow on failure, plus a best-effort sweep of stale `state.json.*.tmp` in `Load()` for the hard-crash case. Cost if wrong: a stray temp file, which is what the fix removes.
Task 5: fix round 2/5 (1 addressed, 0 open; commits f0a6245..8498081) — re-reviewer forced Move and Delete to fail simultaneously (deny-Delete ACE) and confirmed the caller still sees the original exception byte-identically; also confirmed the sweep leaves `state.json` and the `state.json.corrupt-*` quarantine files alone.
Task 5: complete (commits bd93de8..8498081, review clean — 71 asserts, exit 0)

Task 6: review (opus, sandboxed against a fake game folder) — spec ❌, quality NOT approved. 1 Critical, 5 Important, all reproduced. Implementer had already found and fixed four real gaps beyond the brief (mirrored backup path, rollback failures now thrown, non-fatal config backup, self-healing SharedFiles).
Task 6: Ruling (C1): `SetEnabled` rewrites `f.Path`, which is the very key `SharedFiles` is looked up by — so disabling one mod moves a shared library out from under another, and the later `Remove` deletes it while `state.json` still claims it is installed. Silent, no log. Decision: `mod.Files[].Path` becomes the **canonical** path (always the enabled location) and is never rewritten; the physical location is derived from `mod.Enabled`. This also stabilises stored paths across toggles. Cost if wrong: a stored path no longer mirrors the file's current location, so any future code reading it must go through the new helper.
Task 6: Ruling (I1/M4): rollback raised the "your game folder may be damaged" alarm on a pristine folder, for the single most common failure — the game still running. Rollback must compare the target's hash against the backup and only restore, or alarm, when the content actually differs. Cost if wrong: a genuinely damaged file whose hash coincides with the backup would be skipped, which is the same as not needing restoration.
Task 6: Ruling (I2): duplicate targets in one `ops` list re-back-up the just-installed file over the pristine backup, so rollback restores the wrong content. `Apply` is public and must reject duplicate targets itself rather than rely on `PackageMapper`. Cost if wrong: a caller passing intentional duplicates gets an exception instead of an ambiguous install.
Task 6: Ruling (I3): `SetEnabled` overwrites a live file with a stale disabled copy when both exist, unrecoverably. Back the overwritten file up and log it, then proceed — the user's requested action still happens and nothing is destroyed. Cost if wrong: an extra backup file.
Task 6: Ruling (I4): `Remove` enforces no path containment at all, and `ModEntry.Id` — read verbatim from an untrusted mod DLL — is interpolated into both the config source and the backup destination. Reproduced deleting and relocating files outside the game folder. Route every path through the containment check and sanitise the id wherever it becomes a file name. Cost if wrong: a mod whose GUID contains path characters gets a mangled backup file name.
Task 6: Ruling (I5): a pre-existing junction inside the game folder defeats `ResolveInside`, because `Path.GetFullPath` is textual. Re-check containment after resolving link targets. Defence in depth — nothing in this app creates reparse points. Cost if wrong: an installation that deliberately symlinks its plugins folder is refused.
Task 6: Ruling (M1): a game root with a trailing separator makes every install fail with a message that reads like a security rejection. Reachable, because a folder picker returns `C:\` for a drive root and `state.GamePath` is restored verbatim. Normalise it. Cost if wrong: none.
Task 6: Ruling (M2/M3): `InstallRollbackException` carries prose in two incompatible formats with no structured data, so the UI cannot tell the user which files are stuck; it is also thrown from `SetEnabled` despite its name and is undocumented. Give it `AffectedPaths` and `BackupDirectory`, document it, and comment why `Apply`'s catch is deliberately unfiltered. **Carried into Tasks 12/13: the UI must catch this type specifically and surface the affected paths.** Cost if wrong: none.
Task 6: minor (deferred): `CompareVersions` falls back to ordinal comparison, so `10.0.0-rc` loses to `9.0.0`. Bookkeeping only — `RegisterShared` never moves a byte, the last `Apply` physically wins regardless.
Task 6: minor (deferred): `Apply` with an empty op list still creates a backup directory and logs "applied 0 file(s)".
Task 6: minor (deferred): `PruneBackups` has no call site yet, though its doc says it is called at startup — Task 11 wires it into `Program.cs`.
Task 6: minor (deferred): `FileVersionOf` catches only `FileNotFoundException`, deviating from the filtered-catch convention.
Task 6: fix round 1/5 (salvaged after the kill; commits 8f435a0..dd50cb2) — 5 of 8 findings were already correctly implemented by the killed run, 3 needed real work. The audit-don't-trust ruling paid off: the killed run had introduced two new C1 defects (nested plugin paths flattened on re-enable; `PhysicalPath`'s native branch ignoring `file.Path`) and an over-broad I5 that rejected a game folder which is itself a junction.
Task 6: full re-review (opus, sandboxed) of `8498081..dd50cb2` — spec ❌, NOT approved. C1, I1+M4, I2, I3, I4, M1, M2+M3 CLOSED. I5 NOT CLOSED. 1 new Critical, 5 Important, 5 Minor.
Task 6: Ruling (Critical): `Remove` deletes `BepInEx\config\*.cfg` when the archive shipped one, violating the hard "never deleted" invariant — measured: cfg gone, zero files in the backup directory. Exclude config files from both delete loops and route them through `BackupConfig`. Cost if wrong: a config is backed up that the mod would rather have removed, which is the documented behaviour anyway.
Task 6: Ruling (Important 1): C1's bookkeeping half is closed but its physical half is not — `SetEnabled` still moves a shared library away from another enabled mod, because its signature takes no `AppState` and it cannot see the refcount. **Change the signature to `SetEnabled(AppState, GameInstall, ModEntry, bool)`** and skip files another enabled mod still provides. Carried into Tasks 12/13: the UI calls this with the state. Cost if wrong: a file stays in `plugins` that nothing needs, which the next removal cleans up.
Task 6: Ruling (Important 2+3): the reparse check inspects only the target's immediate parent, so a junction at `BepInEx` lets `BepInEx\plugins\Evil.dll` through and outside the folder; `Remove` and `SetEnabled` have no reparse check at all and were shown deleting and importing files across the link. Walk every level from target to root, in one shared helper used by all three. Cost if wrong: an exotic but legitimate symlinked layout is refused, and the message says so.
Task 6: Ruling (Important 4): `SetEnabled`'s rollback catch is filtered and does not catch the `InvalidOperationException` its own path resolution throws, so a mod with one bad entry leaves the first file moved, no rollback, and `Enabled` still true — precisely the drift C1 exists to prevent. Resolve every path up front the way `Apply` does, and match `Apply`'s deliberately unfiltered rollback catch with the same justification comment. Cost if wrong: none.
Task 6: Ruling (Important 5): two DLLs of the same name in different plugin subfolders collapse onto one flat name in `plugins-disabled`, and a disable/enable round trip destroys one — reachable from a single archive. Mirror the subpath under `plugins-disabled` so the toggle becomes a pure prefix swap. Cost if wrong: existing flat `plugins-disabled` files from a prior manual setup are not matched, and are then reported as unmanaged rather than silently clobbered.
Task 6: fix round 2/5 (11 addressed, 1 new Important + 3 new Minors; commits dd50cb2..d5516e7) — re-reviewer drove the code against real `mklink /J` junctions, a real `subst` drive root and real file locks: 90 checks. Reparse walk terminates on a cyclic junction pair, a 40-level path and a UNC-shaped root; 300-file `Apply` costs 323 ms total.
Task 6: Ruling (round 3, Important): the I1 skip orphans a shared file after a full uninstall — `SetEnabled` leaves it physically in `plugins` while setting `Enabled=false`, and `Remove` derives the location purely from `Enabled`, so the last-provider delete aims at a path that does not exist. Result: a DLL BepInEx keeps loading that the manager can never remove. `Remove` must delete whichever of the two physical locations actually exists once the refcount clears. Cost if wrong: none.
Task 6: Ruling (round 3, Minor): `IsProtectedConfig` fails open on `BepInEx\.\config\Dot.cfg` — deleted, no backup. Only reachable from a hand-edited state file, but the Critical ruling said "however it got into `mod.Files`". Compare resolved paths against `game.Config` instead of string prefixes. Cost if wrong: none.
Task 6: Ruling (round 3, Minor): `ResolveInside` dereferences `Path.GetDirectoryName(full)!`, which is null when the resolved path is the root — reachable with a drive-root game folder and an empty stored path, and the resulting `ArgumentNullException` re-opens exactly the permanent `Remove` wedge that Minor 4 closed. Guard it. Cost if wrong: none.
Task 6: Ruling (round 3, Minor): the trailing `BackupConfig(game, mod.Id)` bypasses the refcount, moving a shared config away while another provider remains. Nothing is lost — it is a move — but it should consult the refcount like its sibling loop. Cost if wrong: a shared config stays in place until the last provider goes, which is the intended behaviour anyway.
Task 6: fix round 3/5 (5 addressed, 1 new Important + 1 new Minor; commits d5516e7..a534aaa) — re-reviewer accepted two of three implementer deviations and rejected the third on evidence.
Task 6: Ruling (round 4, Important): the orphan fix deletes "whichever physical location exists", but the second location is a first-class ownable target, not an alias — `PackageMapper` maps `BepInEx/plugins-disabled/Core.dll` and `version.dll_` as ordinary targets. Reproduced: removing mod A deletes mod B's file while B stays in `state.json` pointing at nothing, and a native removal destroys a pre-existing `version.dll_` with no backup. Before deleting a non-canonical candidate, confirm no other `ModEntry.Files` entry and no `SharedFile` resolves to it. Cost if wrong: an orphan survives that the next removal would have cleared — strictly better than deleting a file the manager does not own.
Task 6: Ruling (round 4, Minor): an unresolvable mirror candidate (junctioned `plugins-disabled`) aborts deletion of the canonical file entirely, leaving it on disk while the mod drops out of state — the same drift, through a narrower door. Drop the bad candidate, keep processing the entry.
Task 6: Ruling: rounds 4-5 normally call for a fresh implementer one model tier up. Deviating deliberately — the implementer is already on the most capable model available, and each round's findings have been new consequences of the previous fix rather than one blind spot repeating (11 → 5 → 2 findings, all independently reproduced). Its context is the most valuable asset here. Cost if wrong: a fifth round with fresh eyes, which the cap still allows.
Task 6: Ruling (round 2 Minors, folded in and closed): reject a target whose name is an existing directory up front; make the backup directory id unique rather than millisecond-granular; uniquify the config backup name on clash; make `Remove` log and skip an entry that fails containment instead of wedging permanently on it; restrict `SetEnabled` to files under `BepInEx\plugins\` so a mod shipping `winhttp.dll` cannot disable the whole loader.
Task 6: fix round 4/5 (2 addressed, 0 open; commits a534aaa..31736ae) — re-reviewer built its harness twice, once with only `Installer.cs` reverted to the pre-fix version, and got 16 targeted failures against the old file versus 105 green at HEAD. Differential evidence, not self-confirming. Ship verdict: close.
Task 6: Ruling (round 5, spending the last round deliberately): the reviewer measured that the two asserts round 4 added **pass unchanged against the pre-fix `Installer.cs`** — the suite's fake roots do not exist on disk, so the deletion branch is never reached. The committed regression net therefore pins none of the ~20 findings this task produced; all of that evidence lived in harnesses that are now deleted, for the one component that mutates the user's game folder. The reviewer called fixing it a design change; it is not — Task 8's planned test already builds a real zip under `Path.GetTempPath()`, so the suite does disk I/O already. Add filesystem-backed asserts for the Installer invariants. Cost if wrong: a slower self-test and a temp directory per run.
Task 6: Ruling (round 5, Minor): the ownership scan is genuinely quadratic — measured 21 ms / 63 ms / 1 513 ms / 8 757 ms at 30 / 100 / 400 / 1200 state entries, because each probe is a `GetFullPath` plus a link-resolution walk. Build the owned-path set once per `Remove`. Cost if wrong: none.
Task 6: minor (deferred): `InstalledFile.Sha256` would be a sound discriminator between "my file parked by a disable" and "a stranger's leftover" when deciding whether to delete an unowned alternate — `SetEnabled` moves files and never rewrites them.
Task 6: minor (deferred): two *enabled* mods sharing a canonical path with no `SharedFiles` record still cost one file on removal. Unreachable until the UI tasks call `Apply`/`RegisterShared`.
Task 6: minor (deferred): the toggle log wording changed for the non-removal path too, and backup folders read `removing-`, not `remove-`. Nothing asserts on either.
Task 6: fix round 5/5 (2 addressed, 0 open; commits 31736ae..49d025c) — 23 filesystem asserts added. Final reviewer independently reproduced two historical baselines exactly, mutated HEAD to pin the two requirements that rested on reconstructed shims, and planted real user backups before running the suite: all survived byte-identical. Perf re-measured 3289 ms -> 390 ms at 1200 state entries with semantics checked line by line.
Task 6: complete (commits 8498081..49d025c, review clean — 135 asserts, exit 0, all six required behaviours pinned)
Task 6: carried into Tasks 12/13: `SetEnabled` now takes `AppState` as its first parameter; the UI must catch `InstallRollbackException` and surface `AffectedPaths` + `BackupDirectory`; `Apply`'s pre-flight now throws `InvalidOperationException` where it once threw `ArgumentException`; the mirrored `plugins-disabled\<Sub>` layout replaces the old flat one for any adoption logic.
Task 6: carried into Tasks 12/13 (safety): the self-test's `%LOCALAPPDATA%` snapshot-and-restore has a benign race today only because nothing else writes there — it becomes reachable once the GUI wires `Installer`. Revisit when the UI lands.
Task 6: carried into Task 14 (CI): if the temp directory is unwritable the suite prints "filesystem checks skipped" and still exits 0 with 112 asserts. On CI that is silent green — the workflow should assert the expected count, not just the exit code.
Task 6: minor (deferred): only the `.`-spelled config is filesystem-exercised; the nested round-trip assert is negative-only, so a move to a wrong destination would pass; the `SetEnabled` leg of the escape check passes for a second, independent reason.

Task 7: review (opus, harness against the real validation helpers + a raw-socket server) — spec ✅, quality NOT approved. No security bypass found: the reviewer could not construct a host or redirect chain that escapes the allowlist, and confirmed `Authorization` cannot leak across hops. 5 Important, 12 Minor.
Task 7: implementer closed two real gaps beyond the brief — `HttpClient` auto-follows redirects without re-validating the host (and the reviewer confirmed a real `browser_download_url` does redirect, so this is the normal path, not an edge case), and downloads went straight to the final filename, risking a truncated file indistinguishable from a complete archive.
Task 7: Ruling (I1): `HttpClient.Timeout` does not cover a streamed body under `ResponseHeadersRead` — proven with a server that sends 100 bytes of a declared 10 MB and goes silent: still hanging after 20 s. Since the UI calls this synchronously, that is a permanently frozen application. Add an idle/overall deadline via a linked token source. Cost if wrong: a very slow but healthy download is cancelled and retried.
Task 7: Ruling (I2): disabling auto-redirect on the *shared* client also broke the API path. Live proof: a renamed repo returns 301 to `/repositories/<id>/…`, which falls through to `EnsureSuccessStatusCode` and surfaces raw exception text. Repo renames are routine for mod projects. Give downloads their own handler with manual per-hop validation and leave the API client on auto-redirect. Cost if wrong: none — same-host redirects on api.github.com are safe by construction.
Task 7: Ruling (I3): one `await using (var target = File.Create(tmp))` lacks `ConfigureAwait(false)` — the compiler's `DisposeAsync` captures the context. The reviewer reproduced the deadlock in isolation; it is masked today only because the preceding awaited call already moved off the context. Latent, and a literal violation of the mandatory ruling. Cost if wrong: none.
Task 7: Ruling (I4): every status outside {2xx, 304, 404, 403} and every JSON defect escapes as raw exception text — 401 from an expired PAT, 429 for secondary rate limits (not reported as a rate limit at all), and an HTML error page from a TLS-inspecting proxy yielding a `JsonException` about `'<'`. Map them to English, actionable messages. Cost if wrong: a rarer failure gets a generic message instead of a specific one.
Task 7: Ruling (I5): the security boundary has zero test coverage — the 13 asserts are the brief's, all on the two pure functions, none hostile. Given Task 6's lesson that asserts which pass against the broken code are worse than none, pin the host table. `SelfTest` is in the same assembly, so `internal` costs nothing. Cost if wrong: a slightly larger internal surface.
Task 7: Ruling (Minors, folded in): tighten `github.com` to an exact match (the `"." + host` rule silently allowed all of `*.github.com`); reject a non-standard port; null `RateLimitHint` when the header is absent so it cannot report a reset time in the past; stop reporting every 403 as a rate limit and stop telling a user to add a token they already have; compare `IdnHost`; verify size even when the release omits it; sweep orphaned `*.tmp` in the download directory as `AppState.Load` already does; and add an `InstallableCandidates` helper so Task 13 does not have to re-implement the `.zip`/`.dll` rule and silently diverge from it.
Task 7: fix round 1/5 (5 Important + 13 Minor addressed, 5 small follow-ups open; commits c7f7289..453a0eb) — re-reviewer re-ran two of four mutations and reproduced them exactly, re-measured the full host/redirect table against the real helper, and verified the idle deadline against a genuine `ResponseHeadersRead` stream rather than the implementer's cooperative stub. Ship verdict: ship.
Task 7: Ruling (round 2): the five follow-ups are one-liners and one of them — a mid-transfer drop surfacing as raw `HttpIOException` text — is the most likely real download failure there is. Closing them now is cheaper than leaving five known defects for the final review to rediscover. Cost if wrong: one short extra round.
Task 7: correction to the implementer's own report, established by measurement: `IdnHost` IS discriminable — `https://github。com/x` with U+3002 gives `Host` = `github。com` (rejected) but `IdnHost` = `github.com` (accepted), and accepting is correct because IdnHost is what DNS and TLS use. The change is load-bearing, not correctness-by-argument.
Task 7: finding of record: .NET clears `Authorization` on redirect **even same-origin**. So a renamed repository is re-fetched unauthenticated — a private repo looks like it has no release, and a public one bills the anonymous 60/h bucket, meaning a token does not raise the limit for renamed repos. Not a regression; the fix's comment simply claimed more than it delivers.
Task 7: minor (deferred): automatic redirects on the API client can reach a foreign host, since the framework follows `Location` anywhere. Blast radius is attacker-controlled JSON only — the token does not follow, and `browser_download_url` is still gated by the allowlist.
Task 7: minor (deferred): enabling `AutomaticDecompression` on either handler would immediately produce false "incomplete download" errors, because the Content-Length check compares bytes written against the declared length.
Task 7: fix round 2/5 (5 addressed, 0 open; commits 453a0eb..8657618) — re-reviewer independently reproduced the IDNA discrimination check (reverting `IdnHost` to `Host` fails exactly and only that assert) and traced the narrowed temp-sweep pattern by hand.
Task 7: complete (commits 49d025c..8657618, review clean — 177 asserts, exit 0)
Task 7: minor (deferred): `new FileInfo(tmp).Length` sits in the same handle-released-but-not-yet-moved window as `File.Move` but outside its specific catch, so a concurrent sweep there yields the generic connection message rather than the precise one. Unreachable until something performs concurrent downloads into one folder.

Task 8: review (opus) — spec ✅, quality NOT approved. 3 Important + 1 beyond-spec security gap + 9 Minor. Reviewer fetched only the real archive's central directory via an HTTP Range request, built a byte-faithful 233-entry replica and ran the real `SafeExtract` against it: **accepted, 228 files, all essential paths present** — so the hardening does not refuse the actual runtime.
Task 8: implementer found a real bypass in the brief's own code — `Path.Combine` silently discards the destination root when the entry is rooted, making the plan's escape check circumventable. Fix confirmed smallest-correct and costs no legitimate name.
Task 8: Ruling (I1): a pre-existing **hard link** at a leaf target writes archive content outside the destination — and `mklink /H` needs no elevation, unlike a symlink. `File.GetAttributes` reports no reparse flag, so the leaf check is structurally blind to it. `File.Delete` before `File.Create` closes the hard-link, live-symlink and dangling-symlink cases at once. Cost if wrong: none.
Task 8: Ruling (I2): the zip-bomb second layer is unreachable — `ZipArchiveEntry.Open()` truncates to the declared size, so the streamed count can never exceed it. Measured: an entry declaring 10 bytes while holding 4 MB yields exactly 10 readable bytes. Replace it with caps that can actually fire (per-entry and entry-count). Cost if wrong: none; layer 1 already covers declared totals.
Task 8: Ruling (I3): the reparse check runs in pass 2, so with a junction at `dest\BepInEx` the extractor creates directories **outside** the destination before throwing, and writes earlier legitimate entries first. The self-test misses it only because its archive has a single non-nested entry. Move the check into pass 1 over the existing part of each chain. Cost if wrong: none.
Task 8: Ruling (beyond spec, security): the runtime download uses a default `HttpClient` with auto-redirect on and no host validation, unlike `GitHubClient` which deliberately disables it and re-validates every hop. With no content hash either, a single redirect substitutes an arbitrary archive straight into the live game folder. The real URL answers 200 with no redirect, so pinning the host costs nothing. Cost if wrong: a future hosting change breaks the bootstrap loudly rather than silently accepting a foreign archive.
Task 8: Ruling (Minors, folded in): write `winhttp.dll` and `BepInEx.Core.dll` last so a failure in the final 3.4 MB cannot leave a tree that `Detect` calls healthy; add the duplicate-target and file/directory collision check `PackageMapper` already has; reject DOS device names explicitly rather than by accident; map an over-long name to an English message; log every `SafeExtract` rejection; comment the deliberately bare catch; and require the destination to exist instead of creating a mistyped game path.
Task 8: fix round 1/5 (3 Important + security gap + 9 Minor addressed, 1 open; commits 359152d..043f5c0) — implementer ran 12 separate deliberate reverts, each rebuilt and rerun in isolation, and each produced exactly its own expected failure set. It also retracted its own round-0 claim rather than quietly patching around it.
Task 8: Ruling (round 2): the implementer left M1 open, arguing that writing the two identity files last is only a partial mitigation because a failure landing on `BepInEx.Core.dll` itself could leave a truncated PE with a readable version string. Correct as far as it goes — so the ruling is not "reorder" but "extract both under a temporary name and rename them into place as the last step". A rename within a directory is atomic, so any earlier failure leaves neither file, and a truncated core can never be renamed in. Cost if wrong: a leftover temporary file next to the core assembly, cleaned on the failure paths.
Task 8: Ruling (pace): from Task 9 onward, reviews scale to risk. Tasks 9-11 (log parsing, text redaction, renaming the app's own exe) never touch the game folder — mid-tier models, tighter review prompts, no scoped re-review for mechanical fixes, minors collected for the final review instead of folded into rounds. Tasks 12/13 keep full depth because the UI calls `Installer`. Prompted by the user asking why this is taking so long; the honest answer is that ~110 minutes of Task 6's ~2 hours was review-and-fix cycling, some of it my own breadth rather than the task's risk.
Task 8: fix round 2/5 (1 addressed, 0 open; commits 043f5c0..5d93289) — implementer caught that its own `Detect` assert did not discriminate (it passed for an unrelated reason from round 0 hardening) and strengthened it with real PE bytes until it did. Re-reviewer traced the mechanism and confirmed the pin is non-circular.
Task 8: complete (commits 8657618..5d93289, review clean — 265 asserts, exit 0)
Task 8: minor (deferred): the two identity-file renames are not atomic as a pair, so a locked-file race mid-upgrade could in principle pair a new `winhttp.dll` with an old core. Narrow window — both files are normally locked for the same duration — and a narrowing of the already-accepted "no full transaction" residual, not a new risk class.
Task 9: complete (commit 5f20188) — `LogReader` + `HealthCheck`.
Task 10: complete (commit 25cced0) — `Redactor` + `SupportBundle`. 344 asserts, exit 0. NOT YET REVIEWED.
Task 9/10: implementer found a real bug in the brief's own `SecretAssignment` regex — `\b` treats `_` as a word character, so the pattern never matched the brief's own literal test lines (`ApiKey = …`, `deepl_api_key: …`). It also added a GUID-shaped id pattern, since the given `LongId` pattern cannot see hyphenated GUIDs at all — the standard Unity/BepInEx session id form. Both are real under-redaction gaps in the plan.
Task 11: complete-pending-review (commit 6953bf2) — `SelfUpdate` + single-instance mutex + startup housekeeping. 352 asserts, exit 0. Implementer measured that this build extracts no native libraries to a temp directory, so the running exe is the only locked file — stronger than its own initial assumption, and it corrected the comment to state the measurement.
Task 9/10: review (sonnet, standalone probe harness) — spec ✅, quality NOT approved. 2 Critical, 2 Important. It independently reproduced the implementer's `\b`/underscore finding before accepting it.
Task 9/10: Ruling (C1): the replacement `SecretAssignment` pattern over-redacts destructively — once a keyword matches, the trailing run swallows everything up to the next `:` or `=` anywhere later on the line. Measured: `KeyNotFoundException: The given key was not present…` becomes `KeyNotFoundException: [REDACTED]`, and a stack frame becomes `… in C:[REDACTED]`. A support package exists to carry exactly that; this makes it useless. New defect, not present in the original broken pattern. Require the separator to follow the keyword closely. Cost if wrong: a secret separated from its key by unusual punctuation survives — mitigated by the independent long-id and GUID layers.
Task 9/10: Ruling (C2): `LogReader.ReadTail` and `HealthCheck`'s TOML read catch only `IOException`, but `UnauthorizedAccessException` descends directly from `SystemException` — so an ACL-denied or quarantined file makes `HealthCheck.Run` throw, contradicting its own doc. Brief-inherited, and the same gap was fixed in `SupportBundle` in the same commit. Make the three consistent. Cost if wrong: none.
Task 9/10: Ruling (I3): `APIKEY`, `apikey`, `pw` and `passwd` all leak their values — the first two because the compound detection keys on a case transition that is not there. Cost if wrong: over-masking, which this class explicitly prefers.
Task 9/10: Ruling (I4): underscore-delimited opaque tokens (`sk_live_…`, `ghp_…`) fragment below the 24-character run and survive when they appear without an assignment shape. Cost if wrong: a hyphenated ordinary word gets masked.
Task 9/10: minor (deferred): an unreadable `.version` yields client build "unknown" and then flags every installed mod as stale with a nonsensical message; tail truncation seeks to a raw byte offset and can clip one multi-byte character; `SKIPPED.txt` reports the pre-redaction size; the generated inventory/environment/health files are exempt from the 20 MB budget; multi-line key/value pairs are never redacted; Windows usernames in paths are out of scope by design.
Task 11: review (sonnet, empirical) — spec ✅, quality approved with 2 Important follow-ups. Reviewer independently reproduced the rename-while-running measurement and ran four new experiments of its own.
Task 11: Ruling (Important, trust): GitHub publishes a `"digest": "sha256:…"` field per release asset — the reviewer verified it live against a real repository. So the "nothing to verify against" framing was wrong: no release-process change is needed. Add the field to `ReleaseAsset`, hash the downloaded bytes with `SHA256.HashData` before anything is renamed into place, and refuse on mismatch. This protects **mod downloads too**, not only the self-update, which is the larger surface. Cost if wrong: a release whose digest GitHub omits must fall back to no verification — so treat a missing digest as "unverified" rather than as a failure.
Task 11: Ruling (Important, atomicity): the reviewer measured `kernel32!ReplaceFile` succeeding atomically against a live, running, locked self-contained exe, and separately confirmed that a single `File.Move` over the running exe fails. So the two-`File.Move` window is not unavoidable without a helper process — one `ReplaceFile` call removes it entirely and makes the double-rollback branch dead code. Cost if wrong: `ReplaceFile` failing on an exotic filesystem, which fails closed and leaves the original untouched by definition.
Task 11: minor (deferred): the restart's 5-second mutex wait can time out if the old process shuts down slowly, leaving the user with no running app right after a successful swap; `FindWindowW` matches a bare window title with no uniqueness guard; a failed stale-`.old` delete produces a "may be read-only" message that is plausible but not the actual cause.
Task 11: confirmed not a defect: `PickAsset` not recognising `.exe` does not affect this task — `CheckAsync` never calls it and `ApplyAsync` receives an already-chosen asset. The gap belongs to the future UI caller.
Task 9/10: fix round 1/5 (4 addressed, 1 new Important; commits 6953bf2..ea4dd67) — re-reviewer built its own corpus by copying the real `Redactor.cs` into a harness rather than trusting the asserts, and verified the ACL test sets a genuine NTFS deny rule rather than a file lock.
Task 9/10: Ruling (round 2): the C1 fix restricted the post-keyword gap to whitespace, which broke JSON-shaped lines — `"apiKey": "secretvalue123456"` now leaks completely, where the pre-fix pattern redacted it. The collector gathers no JSON files, but the very mod this class exists for logs JSON bodies into `LogOutput.log` when debugging. Allow an optional quote between keyword and separator, and re-run the full C1 survival corpus to prove it does not reopen the over-redaction. Cost if wrong: a punctuation shape nobody writes gets masked.
Task 9/10: minor (deferred): the `[ \t]{0,8}` cap is functionally inert — the separator group's own `\s*` absorbs any excess; a bare `Bearer <JWT>` with no nearby keyword is only partially masked, a pre-existing limitation of the long-id design.
Task 9/10: fix round 2/5 (1 addressed; commits ea4dd67..6170945) — fixed with a single optional quote before the whitespace gap, verified against the full C1 survival corpus before touching C#.
Ruling (process, mine to own): running two *writing* agents against one working tree was my scheduling choice and it created a real hazard — the second agent used `git stash push --keep-index` / `pop` around a concurrent editor to isolate its commit. It worked and I verified nothing was lost (stash list empty, the other agent's 173 uncommitted lines intact), but a lost update was one mistimed write away. From here: only one writing agent at a time; read-only reviewers may run in parallel. Cost of the rule: slightly less overlap, which is worth it.
Task 9: complete (through commit 6170945, review clean)
Task 10: complete (through commit 6170945, review clean) — re-reviewer ran 37 assertions plus 15 exploratory probes against a byte-identical copy of the real `Redactor.cs`, in both directions.
Task 10: minor (deferred, accepted by design): a quoted non-secret key whose name contains a trigger word now over-masks — `"auth": true` becomes `"auth": [REDACTED]`. Same class as the already-accepted `HotKey = F5` case, and it over-masks rather than leaks. Also pre-existing: a matched value is replaced to end-of-line, so trailing structure on the same line is dropped (`,"other":1}` disappears).
Task 11: complete (commits 6953bf2..7cb3640, review clean — 384 asserts). Reviewer checked the `ReplaceFileW` P/Invoke field by field against the real Win32 signature and confirmed the digest classification lands every malformed input in exactly one of verify / refuse / proceed-unverified without throwing.
Task 11: minor (deferred): `dwReplaceFlags` omits `REPLACEFILE_IGNORE_MERGE_ERRORS`, so an ACL-merge failure can fail an otherwise fine call — fails closed, no data loss.
Task 12/13: implemented (commits f8676d5, 9d411c8) — 388 asserts. NOT YET REVIEWED. Implementer drove real GUI clicks against a scratchpad fake folder for every destructive path and found two real bugs that way: `CheckUpdatesAsync` wrote without checking whether the game was running, and a trust decision was silently lost when a trusted repo's release contained no installable plugin.
Task 12/13: reported Core-layer bug outside its scope, to assess in review: `Path.Combine` without normalisation in `GameInstall.Plugins` and `PruneEmptyToggleDirs` could delete the `plugins` folder if `GamePath` ever contained forward slashes. The INI parser normalises and the folder picker returns backslashes, so no live path produces it — but `state.GamePath` is restored verbatim from a file a user can edit.
Task 12/13: review (opus, harness + live WinForms reproduction) — spec ❌, quality NOT approved. 1 Critical, 9 Important, plus spec gaps. Reviewer confirmed both self-reported bug fixes are real, accepted the "Install BepInEx…" button as justified, and accepted two implementer counter-corrections to my own correction list (`Apply` throws `ArgumentException` for 3 of 4 pre-flight rejections, and `SupportBundle.Create` translates every I/O failure to `InvalidOperationException`, so the brief's filter would never have fired). It read `Installer.cs` rather than trusting me — the right instinct.
Task 12/13: Ruling (C1): a mod adopted from `plugins-disabled` is stored with that non-canonical path, so enabling it moves nothing while the checkbox reports success — silently defeating the core operation on exactly the installations adoption exists to serve. Store the canonical enabled path and let `Enabled=false` express the location. Cost if wrong: none.
Task 12/13: Ruling (I1): `ApplyPackage` stages every entry by file name only, so two archive entries with the same base name in different subfolders overwrite each other in staging and both targets get the same bytes — and the mod can be recorded under the wrong GUID. Same collision class `Installer` already fixed one layer down. Mirror the target's relative directory under staging. Cost if wrong: none.
Task 12/13: Ruling (I2): the running-game gate is checked at click time, but an unbounded modal sits between the check and the write on three paths — file picker, trust dialog, remove confirmation. A user can start the game while the dialog is open. Re-check immediately before each write. Cost if wrong: a redundant check.
Task 12/13: Ruling (I4): closing the BepInEx progress dialog with Alt+F4 returns from `ShowDialog` while the download is still running, and the user is told the install succeeded — during a live 33 MB write into the game folder. Cancel the operation on close instead. Cost if wrong: none.
Task 12/13: Ruling (I6/I7): the display never reconciles a known mod against the disk — a mod moved by hand into `plugins-disabled` still shows enabled, a deleted DLL still shows its old version with no problem row — and adoption is not recursive, so the mirrored subfolder layout the `Installer` produces is invisible. Both make the UI lie about the disk, which is the one thing it must not do. Cost if wrong: none.
Task 12/13: Ruling (spec gaps): the missing affordances (`Update all`, `Remove all mods`, per-mod update, the `LocalMods` folder button, rescan on focus) are spec requirements, not extras — implement them. `Roll back` may need a new `Installer` restore API; if it is not a thin wrapper over the existing backup directory, report why rather than half-building it, and I will surface it as scaled-down scope.
Task 12/13: Ruling (Core bug, overriding the reviewer's "defer"): normalise `Root` in `GameInstall` now. The reviewer downgraded it correctly — the deletion is gated on the directory being empty, so no user data is ever lost, and only a hand-edited `state.json` reaches it — but it is one line, it kills the same latent mismatch in two other helpers, and the whole-branch review should not be making code changes. Cost if wrong: none.
Task 12/13: fix round 1/5 (all 10 findings + spec gaps + the Core normalisation; commit 80c5c70) — 392 asserts. Implementer drove the fixes live against a scratchpad fake folder with a decoy `prime.exe`, and caught two bugs it introduced during the round, one of them via the global exception handler it had just added.
Task 12/13: `Roll back` deliberately not implemented — the implementer reports it needs new `Core` API (`Apply` exposing its backup directory, `ModEntry` remembering it, and a real restore method). Reported rather than half-built, per my ruling. **This is scaled-down scope the user must be told about.**
Task 12/13: residual reported, to assess: recursive adoption can double-adopt a bundled second `BepInPlugin`-tagged DLL that `ApplyPackage` registered only as a `SharedFile`.
Task 14: complete-pending-final-review (commit 85d5c81) — README, licence, CI workflow. Published single-file exe measured at 51,735,197 bytes. CI asserts the expected assert count and the absence of the "filesystem checks skipped" message, so an unwritable temp directory on a runner cannot produce a silent green. Step 4 (repo creation, push, tag) deliberately NOT done by the agent — that is mine to do after the final review.
Task 14: added `permissions: contents: write` (the brief's draft omitted it, and the release action needs it) and a `publish/` gitignore line.
Task 12/13: re-review (opus, A/B against a rebuilt pre-fix commit) — 10 findings closed, 9 fully verified against a real file system. 3 new Important.
Task 12/13: Ruling (round 2, Important): the `Guard()` latch is a regression, and the implementer's own diagnosis was wrong — the reviewer built the pre-fix commit and showed it has no `Activated` handler at all, so that cannot be the cause. `RefreshUi()` inside `Guard()` latches the disabled state and nothing re-polls, so six of nine buttons — including `Rescan` — stay dead after the user closes the game exactly as instructed. A timer that re-polls fixes both directions, since the header is equally stale when the game *starts*. Cost if wrong: a periodic cheap process check.
Task 12/13: Ruling (round 2, Important): the busy lock disables the button panel and mod list only, leaving `Remove all mods` clickable on the other tab and `AllowDrop` live — both reach `Installer` and `Save()` concurrently with an in-flight update.
Task 12/13: Ruling (round 2, Important): the canonical-path contract is still violated on the *install* path — an archive laying a plugin under `plugins-disabled` is stored verbatim, and the new reconcile filter excludes exactly that folder, so the safety net is blind where the original Critical lived.
Task 12/13: Ruling (round 2): the double-adoption ghost IS in scope — one line unioning the shared-file paths into the `known` set, in a file this task owns. The reviewer reproduced it: no data loss, but the ghost returns on every focus regain so "Remove" looks broken, and its checkbox does nothing.
Task 12/13: `Roll back` — reviewer confirmed the assessment is accurate, not an excuse, and estimates one focused task of ~250-400 lines, noting `Apply` must also return the restored-versus-newly-written split because a correct rollback deletes files that did not previously exist. **Surfacing to the user as scaled-down scope.**
Task 12/13: fix round 2/5 (4 addressed; commit c3343b2) — 397 asserts. Verified in the final whole-branch review instead of a separate scoped re-review.
Task 14: complete (commit 85d5c81) — reviewed as part of the final pass.

FINAL WHOLE-BRANCH REVIEW (opus, 34 commits, 115 harness assertions + live message pump): ship verdict **not yet, three cheap must-fixes**. The Core layer that touches the game folder could not be broken — every injected failure left disk and `state.json` in agreement, rollback restored byte-identically, and adoption/toggle/remove round-trip correctly across nested, disabled and shared-file layouts.
MUST FIX 1: the CI self-test step can never pass. `dotnet run` starts a `WinExe` without inherited handles, so `AttachConsole` fails and 0 bytes are captured — reproduced exactly. Every push and PR fails, AND the "filesystem checks skipped" guard is silently vacuous, defeating the Task 6 ruling it was written for. Fix is a `Start-Process … -RedirectStandardOutput` step; the reviewer ran it and got `GREEN: 397 passed`.
MUST FIX 2: spec §9's `Bearer <token>` redaction rule does not exist, and its only assert passes via the unrelated `authorization` keyword — delete the rule you don't have and the test still passes. `Bearer x` (the spec's own test case 4) goes through untouched, in the feature whose entire purpose is being safe to paste into a public channel. Exactly the Task 6 vacuous-assert failure mode.
MUST FIX 3: the round-2 timer defeats the round-2 busy lock, inside the same commit. `RefreshUi()` sets `_removeAllButton.Enabled = !running`, and the new 2-second tick calls it, re-enabling `Remove all mods` mid-operation. `_lastKnownGameRunning` starts null so the first tick always fires — reachable by clicking `Check updates` within two seconds of launch. One line.
FINAL: deferred-list triage — exactly one item must move up (Task 1's "`dotnet run` prints nothing", which Task 14 then built the CI gate on top of; a deferred cosmetic became must-fix 1) and one should (the "unknown" client build message). Everything else is genuinely fine to leave; all Task 11 minors are moot since self-update has no caller.
FINAL: three README claims are false as shipped — `LocalMods` is never scanned, self-update has no entry point, and the BepInEx runtime download is NOT digest-verified (host pinning + size only). Two of those are security claims on an unsigned executable, so they must be corrected or wired up.
FINAL: Roll back assessment confirmed and if anything understated — `ApplyPackage` also overwrites the previous version metadata, and `PruneBackups` deletes after 30 days, so a real implementation needs a "no backup available" path. Shipping without it is defensible: the backups exist for 30 days mirroring the game-folder structure, a manual recovery path works, and nothing promises the feature. Add one README line pointing at the backup folder.
FINAL: nothing in the repository must not be published — no secrets, tokens, usernames or developer paths. `.superpowers/` is untracked; add it to the repo `.gitignore` rather than relying on the nested tooling-generated one.

PAUSED 2026-08-18 by the user after the final review. Resume point: one short fix round for the three must-fixes plus the README corrections, then re-run the self-test, then publish (repo creation, push, tag — the user approved this on 2026-08-16).

POST-RELEASE 2026-08-18: the licence was changed from MIT to GNU GPL v3.0 at the user's
request, after v0.1.1. The plan and the sections above still say MIT because that is what
was decided and built at the time; `LICENSE` and `README.md` are authoritative.
